name: p41_bootstrap
description: "Bootstrap launcher for the standalone P41 desktop app."
publish_to: 'none'
version: 1.1.0+0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.0
  http: ^1.6.0
  path: ^1.9.1
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
